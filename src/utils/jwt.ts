import jwt from 'jsonwebtoken';

export const generateToken = (userId: string) => {
  // Implementar lógica para generar un token JWT
};

export const verifyToken = (token: string) => {
  // Implementar lógica para verificar un token JWT
};